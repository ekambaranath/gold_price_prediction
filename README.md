# gold_price_prediction

This repository is used to predict gold price using a Random Forest Regressor Algorithm.

Random Forest Regressor is an ensemble learning method that predicts continuous values by averaging the outputs of multiple decision trees trained on random subsets of data and features.

Stages:
1. Data collection and Pre-processing
2. Calculating correlation
3. Data split
4. Model training
5. Model Evaluation
